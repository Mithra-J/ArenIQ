# ArenIQ — Python Dependencies
# ================================
# Install all dependencies with:
#   pip install -r requirements.txt
#
# These are required to run:
#   - fetch_sentinel.py  (Sentinel-2 image downloader)
#   - ndwi_detection.py  (NDWI change detection pipeline)
#   - cron_runner.py     (Automated daily runner)

# Satellite image download (Copernicus / Sentinel-2)
sentinelsat==1.3.0

# Geospatial / raster processing
rasterio==1.3.10
shapely==2.0.6

# Image processing (NDWI computation + change detection)
opencv-python==4.10.0.84
numpy==1.26.4

# Machine learning (Random Forest encroachment classifier)
scikit-learn==1.5.2

# Supabase integration (upload results to dashboard)
supabase==2.10.1

# Environment variables
python-dotenv==1.0.1